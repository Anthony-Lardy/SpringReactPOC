import React from "react"

import Template from "../templates/Template";

const Page = (props) => {

    return (
        <div style={{height: "100vh"}}>
            <Template />
        </div>
    )
}; export default Page